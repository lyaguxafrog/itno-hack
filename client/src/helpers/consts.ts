export const routes = {
  profile: '/profile',
  dashboard: '/dashboard',
  project: '/project',
  login: '/',
  register: '/register',
  notFound: '/*',
} as const;

export const links = {
  //
} as const;
